<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- Activar consignacion: agregar stock.group_tracking_owner como
             implied de base.group_user para que todos los usuarios internos
             vean el filtro de propietario en inventario -->
        <record id="base.group_user" model="res.groups">
            <field name="implied_ids" eval="[(4, ref('stock.group_tracking_owner'))]"/>
        </record>

    </data>
</odoo>
