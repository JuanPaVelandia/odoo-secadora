<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Menú Gerencia: submenú dentro de Báscula (como Calidad) -->
        <menuitem id="menu_gerencia_root"
                  name="Gerencia"
                  parent="bascula.menu_bascula_root"
                  sequence="35"
                  groups="secadora_gerencia.group_gerencia,bascula.group_bascula_admin"/>

        <!-- Producción por Lote (pivot/gráfico/lista) -->
        <menuitem id="menu_gerencia_produccion_lote"
                  name="Producción por Lote"
                  parent="menu_gerencia_root"
                  action="secadora_calidad.action_produccion_lote_report"
                  sequence="10"/>

        <!-- Imprimir Producción por Lote (PDF) -->
        <menuitem id="menu_gerencia_produccion_lote_imprimir"
                  name="Imprimir Producción por Lote"
                  parent="menu_gerencia_root"
                  action="secadora_calidad.action_produccion_lote_wizard"
                  sequence="20"/>

    </data>
</odoo>
