<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Vista de formulario de Purchase Order con campo de modo de cálculo de impuestos -->
    <record id="view_purchase_order_form_tax_mode" model="ir.ui.view">
        <field name="name">purchase.order.form.tax.mode</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">

            <!-- Agregar el campo después de la moneda -->
            <xpath expr="//field[@name='currency_id']" position="after">
                <field name="tax_calculation_mode"
                       widget="radio"
                       options="{'horizontal': true}"
                       readonly="state in ('purchase', 'done', 'cancel')"/>
            </xpath>

        </field>
    </record>

</odoo>
